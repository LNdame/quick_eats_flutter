import 'package:flutter/material.dart';

class PreferencesPage extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: Text("Preferences")),
    body: Center(child: Text("Preferences Page to house the settings. Probably will be commented out out for MVP"),),
    );
  }

}